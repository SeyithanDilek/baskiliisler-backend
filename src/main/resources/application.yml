spring:
  datasource:
    url: jdbc:postgresql://baskiliisler-baskiliisler.d.aivencloud.com:14549/defaultdb?sslmode=require
    username: avnadmin
    password: AVNS_cFGK5rlTGxFz9KBaivY
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        id:
          new_generator_mappings: true
          db_structure_naming_strategy: single
        hbm2ddl:
          auto: update
    open-in-view: false

  flyway:
    baseline-on-migrate: true
    enabled: true

server:
  port: 8088

springdoc:
  api-docs:
    path: /api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operationsSorter: alpha
    tagsSorter: alpha
    displayRequestDuration: true
  show-actuator: true

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE

jwt:
  secret: 4b8d7f89e4123cafe7e01a2b9c65f013af45d12fbeef1187c7fa8934e0b3dd13
  expiration: 30m
